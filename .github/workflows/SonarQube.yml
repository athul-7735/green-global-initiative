name: SonarQube Analysis
on:
  workflow_dispatch: # Allows manual triggering from the Actions tab
  push:
    branches:
      # - main
      - NGGI-97-sonar-cube-sonar-lint-integration
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
          
      # Install SonarScanner - Comment line 19 to 24 if we are not using run command
      - name: Install SonarScanner
        run: |
          curl -sL https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.7.0.2747-linux.zip -o sonar-scanner.zip
          unzip sonar-scanner.zip -d $HOME
          echo "$HOME/sonar-scanner-4.7.0.2747-linux/bin" >> $GITHUB_PATH
      
      - name: SonarQube Scan
        #uses: SonarSource/sonarqube-scan-action@v5 #Comment this line if we are going to use Run command
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }} # Comment this line if we ar enot using run command
        run: |
          sonar-scanner \
            -Dsonar.projectKey=JohnKirubaharan_green-global-initiative \
            -Dsonar.organization=johnkirubaharan \
            -Dsonar.sources=. \
            -Dsonar.pullrequest.key=${{ github.event.pull_request.number || '0' }} \
            -Dsonar.pullrequest.branch=${{ github.head_ref || github.ref_name }} \
            -Dsonar.pullrequest.base=${{ github.base_ref || 'main' }} \
            -Dsonar.pullrequest.provider=github \
            -Dsonar.pullrequest.github.repository=${{ github.repository }}