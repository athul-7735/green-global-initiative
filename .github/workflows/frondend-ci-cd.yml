name: Frontend CI/CD Pipeline

on:
  push:
    branches:
      - NGGI-112-ci-yaml-file-integration-with-all-the-tools-frontend-ui
  pull_request:
    branches:
      - NGGI-112-ci-yaml-file-integration-with-all-the-tools-frontend-ui

jobs:
  build:
    runs-on: [self-hosted]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: List files in workspace
      run: |
        ls -R

    - name: Print working directory
      run: pwd

    - name: Navigate to green-global-initiative-ui directory
      run: cd green-global-initiative/green-global-initiative/green-global-initiative-ui

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with: 
        node-version: '18'
        cache: 'npm'

    - name: List project files
      working-directory: ./green-global-initiative/green-global-initiative/green-global-initiative-ui
      run: ls -la

    - name: Install dependencies
      working-directory: ./green-global-initiative/green-global-initiative/green-global-initiative-ui
      run: npm install

    - name: Install Angular CLI locally
      working-directory: ./green-global-initiative/green-global-initiative/green-global-initiative-ui
      run: npm install @angular/cli --save-dev

    - name: Build the project
      working-directory: ./green-global-initiative/green-global-initiative/green-global-initiative-ui
      run: npm run build

    - name: Run Tests in Headless Mode
      working-directory: ./green-global-initiative/green-global-initiative/green-global-initiative-ui
      run: npm test -- --browsers=ChromeHeadless --watch=false || true

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_TOKEN }}

    - name: Trivy File Scan
      working-directory: ./green-global-initiative/green-global-initiative/green-global-initiative-ui
      run: trivy fs . --severity HIGH,CRITICAL --ignore-unfixed > trivyfs-backend.txt

    - name: Build Docker image
      working-directory: ./green-global-initiative/green-global-initiative/green-global-initiative-ui
      run: docker build -t ${DOCKER_HUB_USERNAME}/frontend:${GITHUB_SHA} .

    - name: Trivy Image Scan
      run: trivy image ${DOCKER_HUB_USERNAME}/frontend:${GITHUB_SHA} --severity HIGH,CRITICAL > trivyimage-backend.txt

    - name: Push Docker image
      run: docker push ${{ secrets.DOCKER_HUB_USERNAME }}/frontend:latest

    - name: Run OWASP Dependency Check
      uses: dependency-check/Dependency-Check_Action@main
      with:
        project: 'green-global-initiative'
        format: 'ALL'
        fail_on_cvss: 7

    - name: Upload test results
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: green-global-initiative/green-global-initiative/green-global-initiative-ui/test-results

    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: green-global-initiative/green-global-initiative/green-global-initiative-ui/coverage

    - name: Fail if code coverage is below threshold
      run: |
        if [ "$(echo "$COVERAGE < $COVERAGE_THRESHOLD" | bc)" -eq 1 ]; then
          echo "Code coverage is below the threshold of $COVERAGE_THRESHOLD%"
          exit 1
        fi